# Auto detect text files and perform LF normalization
* text=auto

# Force shell scripts to always use LF line endings
*.sh text eol=lf

# Force batch/powershell files to always use CRLF line endings
*.bat text eol=crlf
*.ps1 text eol=crlf
